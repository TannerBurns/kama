# M2 Acceptance Evidence

**Milestone status: In Progress.** Repository CI and qualifying hosted CPU/Kind
evidence passed for implementation commit
[`31f8291736c299dea7652cc596c6f57f50c52cad`](https://github.com/TannerBurns/kama/commit/31f8291736c299dea7652cc596c6f57f50c52cad),
tested as pull-request merge checkout
`ab03ec8c9acb92927d7807f1618d3ebaf8e7d651`. Published and signed production runtime
qualification, protected real-NVIDIA evidence, and direct system-level coverage of
changed artifact identity/location remain pending, so M2 is not complete.

## Repository verification

Run from a clean checkout:

```sh
make verify-generated
make test
make test-envtest
make helm-validate
make release-check
for minor in 1.34 1.35 1.36; do make test-kind K8S_MINOR="$minor"; done
```

The Kubernetes compatibility workflow also runs
`make test-e2e-serving-cpu K8S_MINOR=<minor>` and
`make verify-e2e-serving-cpu-evidence K8S_MINOR=<minor>` for every 1.34–1.36 matrix
entry. Its stable `M2 CPU/Kind acceptance` job requires all three entries to succeed;
this is the hosted check to require in branch protection. Ordinary pull-request jobs
remain GPU-independent and compile only the lowest/highest CUDA architecture boundaries;
release publication retains the full supported CUDA architecture set and a six-hour job
timeout. The repository release check enforces that split and timeout. The Kind smoke
test performs the documented CRD-first M1-to-M2 upgrade sequence from pinned pre-M2 commit
`5ea80bc6745ddc99a0f70cbc537be95cd31638cb`: it installs that chart and binaries as
release revision 1, applies the M2 CRDs, upgrades the same release to revision 2, and
verifies existing `ModelCache` and `ModelArtifact` UIDs and readiness are preserved.

The qualifying commit must contain the `ModelDeployment` API/webhooks/controller,
supervisor, pinned CPU/CUDA builds, CRD/chart/RBAC integration, examples, and
operational documentation. Record the immutable commit and CI/Kind run URLs below
only after every command succeeds without generated drift.

| Evidence | Result |
|---|---|
| Verified commit | Implementation head [`31f8291`](https://github.com/TannerBurns/kama/commit/31f8291736c299dea7652cc596c6f57f50c52cad); tested merge `ab03ec8c9acb92927d7807f1618d3ebaf8e7d651` |
| Repository CI | [Run 29838617419](https://github.com/TannerBurns/kama/actions/runs/29838617419): [quality/policy](https://github.com/TannerBurns/kama/actions/runs/29838617419/job/88661447958), [race/envtest](https://github.com/TannerBurns/kama/actions/runs/29838617419/job/88661448000), and [images/chart/SBOMs](https://github.com/TannerBurns/kama/actions/runs/29838617419/job/88661448180) passed; [supply-chain artifact 8499609283](https://github.com/TannerBurns/kama/actions/runs/29838617419/artifacts/8499609283), digest `sha256:4e8f94a9919aa751b533729e38fd271d3ce27ff0697307b6544ac957fa8e75e8` |
| Kubernetes 1.34 Kind | [Job 88661478827](https://github.com/TannerBurns/kama/actions/runs/29838617435/job/88661478827); [artifact 8498972051](https://github.com/TannerBurns/kama/actions/runs/29838617435/artifacts/8498972051) |
| Kubernetes 1.35 Kind | [Job 88661478882](https://github.com/TannerBurns/kama/actions/runs/29838617435/job/88661478882); [artifact 8498945414](https://github.com/TannerBurns/kama/actions/runs/29838617435/artifacts/8498945414) |
| Kubernetes 1.36 Kind | [Job 88661478836](https://github.com/TannerBurns/kama/actions/runs/29838617435/job/88661478836); [artifact 8499126464](https://github.com/TannerBurns/kama/actions/runs/29838617435/artifacts/8499126464) |

The stable [M2 CPU/Kind acceptance job](https://github.com/TannerBurns/kama/actions/runs/29838617435/job/88667899166)
passed after all three matrix entries succeeded.

The repository controller/envtest result must also prove that a transient readiness
outage for an unchanged artifact preserves the already-loaded Pod, reports
`ArtifactReady=False`, and freezes replacement scheduling. Current runtime state and
EndpointSlice readiness, rather than the artifact condition alone, continue to decide
whether that preserved endpoint is serving. A changed artifact UID, digest, location,
or model reference must instead begin drain-first replacement.

The current race-enabled controller unit test proves that an unchanged artifact outage
preserves the loaded workload and freezes replacement scheduling. Fingerprint tests
cover digest, location, and model-reference changes, while the Kind drain test proves
the generic fingerprinted replacement path. There is not yet a direct envtest or Kind
transition that changes artifact UID/digest/location and asserts the resulting endpoint
state. The compound artifact/failure definition-of-done item therefore remains open,
even though the never-loaded and terminal-load-failure subcases below have qualifying
Kind evidence.

## CPU serving and failure evidence

Run the production CPU runtime suite:

```sh
make test-e2e-serving-cpu K8S_MINOR=1.36
make verify-e2e-serving-cpu-evidence K8S_MINOR=1.36
```

It must use the pinned loadable SmolLM2-360M-Instruct GGUF and production CPU image,
then retain redacted evidence under `dist/e2e/serving-cpu`. One run must prove:

- the artifact UID/digest/location gates workload creation and the mounted path is
  read-only;
- a bounded Pod using Kama's static serving client reaches the model through its
  ClusterIP Service DNS, observes nonempty generated assistant content in a valid
  OpenAI-compatible SSE chunk, and then observes the terminal `[DONE]` event;
- the CPU runtime image evidence contains a BuildKit-produced immutable manifest
  digest and OCI revision/source labels matching the checkout; a functional run with
  no recoverable manifest digest records `qualifying=false`, and hosted CPU jobs
  reject that result. The disposable Kind cluster loads those content-addressed
  Buildx images under run-local tags with `imagePullPolicy: Never`; the recorded
  manifest digests and matching OCI labels, not those isolated transport tags, are
  the immutable qualification identities;
- CPU mode reports `Serving=True` and `Degraded=True` with
  `CPUOnlyRequested`;
- delayed loading creates no ready EndpointSlice address until the child is ready;
- an invalid artifact or terminal load failure leaves the supervisor running,
  unready, with container restart count zero and no ready endpoint; and
- update/deletion observes the first SSE event, proves the request is still active
  and incomplete, then observes Pod and EndpointSlice readiness removal before the
  stream completes within the declared deadline.

| Evidence | Result |
|---|---|
| CPU workflow/run/job | [Kind run 29838617435](https://github.com/TannerBurns/kama/actions/runs/29838617435); [aggregate job 88667899166](https://github.com/TannerBurns/kama/actions/runs/29838617435/job/88667899166) |
| Redacted artifact | [Kubernetes 1.34](https://github.com/TannerBurns/kama/actions/runs/29838617435/artifacts/8498972051), [1.35](https://github.com/TannerBurns/kama/actions/runs/29838617435/artifacts/8498945414), and [1.36](https://github.com/TannerBurns/kama/actions/runs/29838617435/artifacts/8499126464); retained for 90 days |
| Manager, importer, serving-client, CPU runtime, and model digests | Recorded per Kubernetes version below |

Each artifact reports `qualifying=true`, complete/redacted evidence, verified immutable
image provenance, llama.cpp commit
`af6528e6df5d798f7f1363ec1141699be0f638e2`, build `9445`, and source archive SHA-256
`8bb78d0331d7be27fae4321977eb5f3c686af85cae13c74e6a6b9150e90e4f18`.
The matched client/server versions and observed generated assistant output were:

| Kubernetes | Client/server | Generated output before `[DONE]` |
|---|---|---|
| 1.34 | `v1.34.8` / `v1.34.8` | 16 fragments, 68 bytes |
| 1.35 | `v1.35.5` / `v1.35.5` | 2 fragments, 6 bytes |
| 1.36 | `v1.36.1` / `v1.36.1` | 2 fragments, 3 bytes |

| Kubernetes | Manager digest | Importer digest | Serving-client digest | CPU runtime digest | Model digest |
|---|---|---|---|---|---|
| 1.34 | `sha256:93e440f38b7fc7d03464bea42eb29cf0ef0e50354760f2dfa5b474d9012c8011` | `sha256:8b5c85812d3fa169f0d868b016256fbfac04b12d4e2a5b889aee7583ced502d3` | `sha256:d005605b1a281718917a19fe275bab9e14773d57842a26397c8fd24714061697` | `sha256:acd60c996211a2d1c509e3c9963f322fe57f911687b870217235bbebf58b5b31` | `sha256:48ab3034d0dd401fbc721eb1df3217902fee7dab9078992d66431f09b7750201` |
| 1.35 | `sha256:596e8bc9edbbd8d72c545baac7ffd2c1bb918160ac45377cf5a55d37e1642f00` | `sha256:00cb3739f1599ba8a39cbc239d9949bd31c5db0ee26002b9cfae4b057aa5dae0` | `sha256:0adcb292d84e6ac9b68b560e0dbc889c7a42375c115f260c68f1debc6c298168` | `sha256:21550b8dd0b45aed3687c7cac7f5a8fc65660d952c252eadbb5baef8d083dc77` | `sha256:48ab3034d0dd401fbc721eb1df3217902fee7dab9078992d66431f09b7750201` |
| 1.36 | `sha256:0ab82d42e095438462c48eb000e3033bf481b182769e090ba541db6d5bec910c` | `sha256:c8151eab13f3c13e0a9044cb1c7ecbe2a3d3425b92b6e818743e9e61bd006296` | `sha256:3982c9fe78cf1949e3feb9b235777694f457ad1740ca02fe79c7bfd595d21b05` | `sha256:e5fe34e90cb9bf26ac8a2946c1049a6f645194be4bb8828fba75cc707823b077` | `sha256:48ab3034d0dd401fbc721eb1df3217902fee7dab9078992d66431f09b7750201` |

## Protected NVIDIA acceptance gate

Run only trusted `main` code on the protected runner/cluster:

```sh
make test-e2e-serving-nvidia
```

The runner labels are `[self-hosted, Linux, X64, kama-nvidia]`. Prefer an ephemeral
runner; otherwise configure its runner group and cluster credentials for this
repository only. Restrict the protected `e2e-nvidia` environment to the `main`
deployment branch with required reviewers. The environment provides a dedicated
kubeconfig for a supported Kubernetes 1.34, 1.35, or 1.36 cluster without exposing
it to pull requests. The workflow writes it to a
run-specific mode-`0600` file and removes it in an unconditional cleanup step. A
hosted validation job rejects manual dispatches targeting any non-`main` ref, so a
skipped GPU job cannot produce a false-green qualification. The protected job checks
out the exact
`E2E_NVIDIA_EXPECTED_COMMIT` with full history and requires that commit to be an
ancestor of the current `origin/main` before it receives the kubeconfig or uses the
GPU cluster. Make the stable `Protected NVIDIA acceptance gate` check required for
M2 promotion.
The suite must retain `dist/e2e/serving-nvidia` and prove:

- the generated Pod requests and receives exactly one full `nvidia.com/gpu`;
- the CUDA runtime observes exactly one visible GPU, reports a nonempty device
  identity, and reports `offloadedLayers == totalLayers > 0`;
- a bounded in-cluster Pod using the immutable Kama serving-client fixture reaches the
  verified model through its ClusterIP Service DNS, observes nonempty generated
  assistant content in a valid OpenAI-compatible SSE chunk, and observes the terminal
  `[DONE]` event;
- `RuntimeReady` and `Serving` describe the current artifact/runtime fingerprint,
  status names and UIDs match the exact artifact, Deployment, and Service objects,
  desired/observed/loaded fingerprints agree, desired and observed CUDA image
  identities agree with the selected manifest, effective context/concurrency are
  `2048`/`1`, desired/ready replicas are `1`/`1`, and the Pod has no unexpected
  restarts;
- the suite queries the allocated container for its actual GPU and driver, reads the
  installed CUDA version, validates the installed CUDA 12.4 runtime package, and
  verifies that `llama-server` links to both CUDA runtime and BLAS libraries instead
  of trusting supplied labels. The evidence records those facts, Kubernetes,
  llama.cpp commit, manager/runtime/model digests, sanitized state, workload
  security/resources/image identity, Pod UID/node/readiness/restart count,
  EndpointSlice readiness, and request metadata; and
- every production image and the serving-client fixture image have OCI source and
  revision labels matching the trusted checkout, both runtime images match the
  llama.cpp pin, and the CUDA image matches the CUDA pin. Each reference must use its
  canonical `ghcr.io/tannerburns/kama-*` repository at an immutable digest. Cosign
  must verify a keyless release signature and SPDX JSON attestation for every image,
  bound to Kama's trusted release workflow and the expected commit. Missing
  inspection, signature, attestation, CUDA, or linkage evidence retains the
  functional result with `qualifying=false` and fails qualification.

After the hardware suite, run the shared cross-file verifier (the workflow does this
in an `always()` step so incomplete evidence cannot pass silently):

```sh
make verify-e2e-serving-nvidia-evidence
```

Configure the protected `e2e-nvidia` environment with base64-encoded kubeconfig
Secret `KUBECONFIG_BASE64` and these non-secret variables. Every image value is an
immutable `repository@sha256:<digest>` reference:

| Variable | Contract |
|---|---|
| `E2E_NVIDIA_EXPECTED_COMMIT` | Exact trusted commit represented by the images; checked out directly and required to be an ancestor of current `origin/main` |
| `E2E_NVIDIA_MANAGER_IMAGE` | Manager image digest |
| `E2E_NVIDIA_IMPORTER_IMAGE` | Importer image digest |
| `E2E_NVIDIA_FIXTURES_IMAGE` | Test-fixture image digest containing the static serving client |
| `E2E_NVIDIA_RUNTIME_CPU_IMAGE` | CPU runtime image digest configured alongside CUDA |
| `E2E_NVIDIA_RUNTIME_CUDA_IMAGE` | CUDA runtime image digest under test |
| `E2E_NVIDIA_STORAGE_CLASS` | Disposable RWO-capable class on the dedicated cluster |
| `E2E_NVIDIA_DRIVER_VERSION` | Expected NVIDIA driver version; must equal the runtime-container query |
| `E2E_NVIDIA_CUDA_VERSION` | Expected runtime-container CUDA version; exactly `12.4.1` for M2 |
| `E2E_NVIDIA_MODEL_REVISION` | `593b5a2e04c8f3e4ee880263f93e0bd2901ad47f` |
| `E2E_NVIDIA_MODEL_SHA256` | `48ab3034d0dd401fbc721eb1df3217902fee7dab9078992d66431f09b7750201` |
| `E2E_NVIDIA_MODEL_SIZE` | `386404992` |

| Evidence | Result |
|---|---|
| NVIDIA workflow/run/job | Pending |
| Redacted artifact | Pending |
| GPU/driver/CUDA and production/serving-client immutable digests | Pending |

## Completion rule

Repository deliverable checkboxes may be checked once that deliverable is implemented
and its automated repository gates pass; an unchecked runtime-image deliverable remains
pending until the production images are built and qualified. M2 remains `In Progress`
until all repository, CPU/failure, and real-NVIDIA evidence tables are complete. Then
update the delivery roadmap in an evidence-only follow-up:

1. check any remaining deliverables and all three definition-of-done entries;
2. set `Status: Complete (YYYY-MM-DD)`;
3. link the immutable implementation commit and CI, Kind, CPU, and NVIDIA runs; and
4. record the published CPU/CUDA image and model digests.

M1 fixtures are prerequisites, not M2 evidence. A successful fake llama-server test,
CUDA image build, or protected workflow definition does not qualify the NVIDIA gate.
